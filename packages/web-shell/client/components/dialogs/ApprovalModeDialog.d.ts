interface ApprovalModeDialogProps {
    currentMode: string;
    onSelect: (modeId: string) => void;
}
export declare function ApprovalModeDialog({ currentMode, onSelect, }: ApprovalModeDialogProps): import("react").JSX.Element;
export {};
