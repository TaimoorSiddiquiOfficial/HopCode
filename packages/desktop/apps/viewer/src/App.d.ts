/**
 * HopCode Session Viewer
 *
 * A minimal web app for viewing HopCode session transcripts.
 * Users can upload session JSON files or view shared sessions via URL.
 *
 * Routes:
 * - / - Upload interface
 * - /s/{id} - View shared session
 */
export declare function App(): import("react").JSX.Element;
