import type { RpcServer } from '@craft-agent/server-core/transport';
import type { HandlerDeps } from './handler-deps';
export declare const GUI_HANDLED_CHANNELS: readonly [any, any];
export declare function registerSettingsGuiHandlers(server: RpcServer, _deps: HandlerDeps): void;
