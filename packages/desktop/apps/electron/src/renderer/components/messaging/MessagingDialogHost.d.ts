/**
 * MessagingDialogHost
 *
 * Global host that owns the messaging pairing/connect dialogs so they survive
 * the close of the triggering context menu or dropdown.
 */
export declare function MessagingDialogHost(): import("react").JSX.Element;
