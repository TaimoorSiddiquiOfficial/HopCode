import type { SessionMeta } from '@/atoms/sessions';
interface SessionBadgesProps {
    item: SessionMeta;
}
export declare function SessionBadges({ item }: SessionBadgesProps): import("react").JSX.Element | null;
export {};
