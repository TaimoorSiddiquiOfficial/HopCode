#!/usr/bin/env bun
/**
 * Debug script to print the full HopCode system prompt with annotations.
 * Shows both the static system prompt and dynamic user message context components.
 *
 * Run with: bun run print:system-prompt
 */
export {};
