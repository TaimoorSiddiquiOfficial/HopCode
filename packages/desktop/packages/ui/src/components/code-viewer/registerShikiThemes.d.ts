/**
 * Register craft-dark / craft-light Shiki themes once per runtime.
 * Prevents duplicate registration warnings during HMR or StrictMode re-mounts.
 */
export declare function registerCraftShikiThemes(): void;
